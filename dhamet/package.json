{
  "name": "dhamet-cloudflare-final-release",
  "version": "1.0.2",
  "private": true,
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "deploy:pages": "node deploy/deploy-pages.mjs",
    "deploy:worker": "node deploy/deploy-worker.mjs",
    "deploy": "npm run deploy:pages && npm run deploy:worker"
  },
  "devDependencies": {
    "wrangler": "latest"
  },
  "cloudflare": {
    "pagesProjectName": "dhamet2",
    "d1DatabaseName": "dhamet2_test"
  }
}
